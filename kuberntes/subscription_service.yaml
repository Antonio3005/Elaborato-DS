apiVersion: apps/v1
kind: Deployment
metadata:
  name: subscription-service
spec:
  replicas: 3
  selector:
    matchLabels:
      app: subscription-service
  template:
    metadata:
      labels:
        app: subscription-service
    spec:
      containers:
        - name: subscription-service
          image: elaborato-ds-subscription_service:latest
          ports:
            - containerPort: 5001
          env:
            - name: MYSQL_DATABASE
              value: subscription
            - name: MYSQL_USER
              value: an
            - name: MYSQL_PASSWORD
              value: '12345'
            - name: MYSQL_ROOT_PASSWORD
              value: '12345'
            - name: SECRET_KEY
              value: chiavesegreta
            - name: KAFKA_BOOTSTRAP_SERVERS
              value: 'kafka:9092'

---
apiVersion: v1
kind: Service
metadata:
  name: subscription-service
spec:
  selector:
    app: subscription-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5001
