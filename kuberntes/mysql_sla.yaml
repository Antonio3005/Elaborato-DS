apiVersion: v1
kind: Service
metadata:
  name: mysql-sla
  namespace: my-namespace
spec:
  selector:
    app: mysql-sla
  ports:
    - protocol: TCP
      port: 3306
      targetPort: 3306

---

apiVersion: apps/v1
kind: Deployment
metadata:
  name: mysql-sla
  namespace: my-namespace
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mysql-sla
  template:
    metadata:
      labels:
        app: mysql-sla
    spec:
      containers:
        - name: mysql-sla
          image: mysql:latest
          env:
            - name: MYSQL_ROOT_PASSWORD
              value: "12345"
            - name: MYSQL_DATABASE
              value: "sla"
            - name: MYSQL_USER
              value: "an"
            - name: MYSQL_PASSWORD
              value: "12345"
          ports:
            - containerPort: 3306
          volumeMounts:
            - name: mysql-data-sla
              mountPath: /var/lib/mysql
      volumes:
        - name: mysql-data-sla
          persistentVolumeClaim:
            claimName: mysql-data-sla

